/** @deprecated Prefer `@/lib/platform` — kept for accidental imports. */
export {
  hasTelegramSignedInitData,
  isTelegramMiniApp,
  waitForTelegramMiniApp,
  persistTelegramLaunchHash,
} from './platform'
